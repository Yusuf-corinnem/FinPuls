# Сущности Backend модуля

Детальное описание всех Entity классов для основного Backend модуля.

---

## 📋 Список сущностей

1. **User** - Пользователь
2. **UserCredentials** - Учетные данные пользователя
3. **Token** - Refresh токен для JWT
4. **UserBankClient** - Связь пользователя с клиентами банков (поддержка нескольких банков)

---

## 1. User (Пользователь)

### Назначение
Хранит основную информацию о пользователях системы.

### Поля

| Поле | Тип | Описание | Обязательное | Уникальное |
|------|-----|----------|---------------|------------|
| `id` | `Long` | Первичный ключ | ✅ | ✅ |
| `username` | `String` | Имя пользователя | ✅ | ✅ |
| `email` | `String` | Email адрес | ❌ | ❌ |
| `firstName` | `String` | Имя | ❌ | ❌ |
| `lastName` | `String` | Фамилия | ❌ | ❌ |
| `createdAt` | `LocalDateTime` | Дата создания | ✅ | ❌ |
| `updatedAt` | `LocalDateTime` | Дата обновления | ❌ | ❌ |

### Связи
- **OneToOne** с `UserCredentials` - один пользователь имеет одни учетные данные
- **OneToMany** с `UserBankClient` - один пользователь может иметь несколько связей с банками
- **Связь с Bank Module** через `UserBankClient` - поддержка нескольких банков для одного пользователя

### Связь с Bank Module
- Связь происходит через промежуточную таблицу `UserBankClient`
- Один пользователь может иметь счета в нескольких банках одновременно
- Каждый Bank Module остается полностью изолированным
- Связь: `User` → `UserBankClient` → `Client.id` (только через ID, без FK)

### Примеры значений

```java
username: "ivan_ivanov"
email: "ivan@example.com"
firstName: "Ivan"
lastName: "Ivanov"
createdAt: 2025-01-20T10:00:00
```

### Индексы
- `username` - уникальный индекс
- `email` - индекс для быстрого поиска (если нужен)

### SQL схема

```sql
CREATE TABLE users (
    id BIGSERIAL PRIMARY KEY,
    username VARCHAR(100) NOT NULL UNIQUE,
    email VARCHAR(255),
    first_name VARCHAR(100),
    last_name VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Связь с Bank Module
- Связь происходит через промежуточную таблицу `user_bank_clients`
- Один пользователь может иметь счета в нескольких банках
- Bank Module **не знает** о существовании Backend - полная изоляция

---

## 2. UserCredentials (Учетные данные)

### Назначение
Хранит логин и пароль пользователя отдельно от основной информации.

### Поля

| Поле | Тип | Описание | Обязательное | Уникальное |
|------|-----|----------|---------------|------------|
| `id` | `Long` | Первичный ключ | ✅ | ✅ |
| `login` | `String` | Логин для входа | ✅ | ✅ |
| `password` | `String` | Хэшированный пароль | ✅ | ❌ |
| `userId` | `Long` | ID пользователя | ✅ | ✅ |
| `createdAt` | `LocalDateTime` | Дата создания | ✅ | ❌ |
| `updatedAt` | `LocalDateTime` | Дата обновления | ❌ | ❌ |

### Связи
- **OneToOne** с `User` - учетные данные принадлежат одному пользователю

### Примеры значений

```java
login: "ivan_ivanov"
password: "$2a$10$..."  // BCrypt хэш
userId: 1
createdAt: 2025-01-20T10:00:00
```

### Безопасность
- **password** должен храниться в **хэшированном виде** (BCrypt или SHA-256)
- Никогда не храните пароли в plain text!
- Используйте `PasswordEncoder` для хэширования

### Индексы
- `login` - уникальный индекс
- `user_id` - уникальный индекс (один пользователь = одни учетные данные)

### SQL схема

```sql
CREATE TABLE user_credentials (
    id BIGSERIAL PRIMARY KEY,
    login VARCHAR(100) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    user_id BIGINT NOT NULL UNIQUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);
```

---

## 3. Token (Refresh токен)

### Назначение
Хранит refresh tokens для обновления JWT access tokens.

### Поля

| Поле | Тип | Описание | Обязательное | Уникальное |
|------|-----|----------|---------------|------------|
| `id` | `Long` | Первичный ключ | ✅ | ✅ |
| `userId` | `Long` | ID пользователя | ✅ | ❌ |
| `refreshToken` | `String` | Refresh token | ✅ | ✅ |
| `expiresAt` | `LocalDateTime` | Время истечения | ✅ | ❌ |
| `createdAt` | `LocalDateTime` | Дата создания | ✅ | ❌ |

### Связи
- **Нет прямых связей** - userId хранится как Long (пользователь в другой БД)

### Примеры значений

```java
userId: 1
refreshToken: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
expiresAt: 2025-01-21T10:00:00
createdAt: 2025-01-20T10:00:00
```

### Важные замечания
- **userId** - это **НЕ внешний ключ** (FK), так как пользователи хранятся в другой БД (PostgreSQL)
- Хранится просто как `Long` для связи по логике приложения
- **refreshToken** должен быть уникальным
- При истечении токена он должен быть удален

### Индексы
- `refresh_token` - уникальный индекс
- `user_id` - индекс для быстрого поиска по пользователю
- `expires_at` - индекс для очистки истекших токенов

### SQL схема (H2)

```sql
CREATE TABLE tokens (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    refresh_token VARCHAR(500) NOT NULL UNIQUE,
    expires_at TIMESTAMP NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_refresh_token (refresh_token),
    INDEX idx_expires_at (expires_at)
);
```

---

## 4. UserBankClient (Связь пользователя с банками)

### Назначение
Хранит связи между пользователями и клиентами банков. Позволяет одному пользователю иметь счета в нескольких банках одновременно.

### Поля

| Поле | Тип | Описание | Обязательное | Уникальное |
|------|-----|----------|---------------|------------|
| `id` | `Long` | Первичный ключ | ✅ | ✅ |
| `userId` | `Long` | ID пользователя | ✅ | ❌ |
| `bankClientId` | `Long` | ID клиента в Bank Module | ✅ | ❌ |
| `bankName` | `String` | Название банка (vbank, abank, sbank) | ✅ | ❌ |
| `isPrimary` | `Boolean` | Основной банк пользователя | ✅ | ❌ |
| `createdAt` | `LocalDateTime` | Дата создания | ✅ | ❌ |
| `updatedAt` | `LocalDateTime` | Дата обновления | ❌ | ❌ |

### Связи
- **ManyToOne** с `User` - связь принадлежит одному пользователю
- **Связь с Bank Module** через `bankClientId` - ID клиента в Bank Module (не FK)

### Примеры значений

```java
userId: 1
bankClientId: 100  // ID клиента в vbank
bankName: "vbank"
isPrimary: true
createdAt: 2025-01-20T10:00:00
```

### Индексы
- `user_id` - индекс для быстрого поиска по пользователю
- `bank_client_id` - индекс для связи с клиентом банка
- `bank_name` - индекс для фильтрации по банку
- `(user_id, bank_client_id)` - уникальная комбинация

### SQL схема

```sql
CREATE TABLE user_bank_clients (
    id BIGSERIAL PRIMARY KEY,
    user_id BIGINT NOT NULL,
    bank_client_id BIGINT NOT NULL,
    bank_name VARCHAR(100) NOT NULL,
    is_primary BOOLEAN NOT NULL DEFAULT false,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    UNIQUE (user_id, bank_client_id),
    INDEX idx_user_id (user_id),
    INDEX idx_bank_client_id (bank_client_id),
    INDEX idx_bank_name (bank_name)
);
```

### Связь с Bank Module
- `bankClientId` - ID клиента в Bank Module (таблица `clients`)
- Это **НЕ внешний ключ** (FK), так как клиенты хранятся в другой БД
- Хранится просто как `Long` для связи по логике приложения
- Bank Module **не знает** о существовании Backend - полная изоляция
- Связь: `UserBankClient.bank_client_id` → `Client.id` (только через ID)

### Логика работы
- Один пользователь может иметь несколько записей в `user_bank_clients`
- Каждая запись связывает пользователя с клиентом в конкретном банке
- Поле `isPrimary` указывает основной банк пользователя
- Поле `bankName` идентифицирует банк (vbank, abank, sbank)

---

## 🔗 Связи между сущностями

```
User (1) ──< (1) UserCredentials
User (1) ──< (Many) Token (только userId, не FK)
User (1) ──< (Many) UserBankClient
UserBankClient.bank_client_id → Client.id (Bank Module, только ID, не FK)
```

### Диаграмма связей

```
┌─────────────┐
│    User     │
│             │
│  - id       │
│  - username │
└──────┬──────┘
       │
       │ 1
       │
       │ 1
       │
┌──────▼──────────────┐
│ UserCredentials     │
│                     │
│  - id               │
│  - login            │
│  - password (hash)  │
│  - userId (FK)      │
└─────────────────────┘

┌─────────────┐
│    User     │
│             │
│  - id       │
└──────┬──────┘
       │
       │ 1
       │
       │ Many
       │
┌──────▼──────────────┐
│      Token          │
│                     │
│  - id               │
│  - userId (Long)    │
│  - refreshToken     │
│  - expiresAt        │
└─────────────────────┘

┌─────────────┐
│    User     │
│             │
│  - id       │
│  - username │
└──────┬──────┘
       │
       │ 1
       │
       │ Many
       │
┌──────▼──────────────────┐
│  UserBankClient         │
│                         │
│  - id                   │
│  - userId (FK)          │
│  - bankClientId         │
│  - bankName             │
│  - isPrimary            │
└──────┬──────────────────┘
       │
       │ (только ID, не FK)
       │
       │
┌──────▼──────────────────────────────┐
│         BANK MODULE                 │
│                                     │
│  ┌─────────────┐                   │
│  │   Client    │                   │
│  │             │                   │
│  │  - id       │◄──────────────────┘
│  │  - firstName│                   │
│  │  - lastName │                   │
│  └─────────────┘                   │
│                                     │
└─────────────────────────────────────┘
```

### Примеры связей

**Один пользователь → Несколько банков:**
```
User (id=1, username="ivan")
  ├── UserBankClient (id=1, userId=1, bankClientId=100, bankName="vbank", isPrimary=true)
  ├── UserBankClient (id=2, userId=1, bankClientId=200, bankName="abank", isPrimary=false)
  └── UserBankClient (id=3, userId=1, bankClientId=300, bankName="sbank", isPrimary=false)

Bank Module (vbank):
  Client (id=100) → Accounts → Cards → Transactions

Bank Module (abank):
  Client (id=200) → Accounts → Cards → Transactions

Bank Module (sbank):
  Client (id=300) → Accounts → Cards → Transactions
```

---

## 📝 Важные замечания

### 1. Разделение User и UserCredentials
- **User** - основная информация (без пароля)
- **UserCredentials** - отдельная таблица для безопасности
- Позволяет легко управлять паролями и логинами отдельно

### 2. Безопасность паролей
- **password** должен храниться в **хэшированном виде**
- Используйте BCrypt или SHA-256
- Никогда не логируйте пароли!

### 3. Токены в H2
- **Token** хранится в H2 (in-memory или file-based)
- **userId** - не FK, так как User в другой БД
- При истечении токена он должен быть удален

### 4. Уникальность
- `username` - должен быть уникальным
- `login` - должен быть уникальным
- `refreshToken` - должен быть уникальным
- `userId` в UserCredentials - должен быть уникальным (один пользователь = одни учетные данные)

### 5. Индексы
- Создайте индексы на часто используемых полях
- Особенно важны индексы на `username`, `login`, `userId`, `refreshToken`

---

## 🚀 Опциональные поля (можно добавить позже)

### User
- `phone` - телефонный номер
- `avatar` - URL аватара
- `birthDate` - дата рождения
- `isActive` - активен ли пользователь
- `lastLoginAt` - дата последнего входа

### UserBankClient
- `lastUsedAt` - дата последнего использования банка
- `preferences` - JSON с настройками для конкретного банка
- `status` - статус связи (ACTIVE, SUSPENDED, CLOSED)

### UserCredentials
- `passwordChangedAt` - дата изменения пароля
- `failedLoginAttempts` - количество неудачных попыток входа
- `lockedUntil` - заблокирован до (для защиты от брутфорса)

### Token
- `deviceInfo` - информация об устройстве
- `ipAddress` - IP адрес
- `userAgent` - User-Agent браузера

---

## 📊 Примеры запросов

### Получить пользователя по username
```java
Optional<User> user = userRepository.findByUsername(username);
```

### Получить все банки пользователя
```java
List<UserBankClient> userBanks = userBankClientRepository.findByUserId(userId);
// Результат: список всех банков, в которых у пользователя есть счета
```

### Получить основной банк пользователя
```java
Optional<UserBankClient> primaryBank = userBankClientRepository.findByUserIdAndIsPrimaryTrue(userId);
```

### Получить связь с конкретным банком
```java
Optional<UserBankClient> bankClient = userBankClientRepository.findByUserIdAndBankName(userId, "vbank");
```

### Получить все связи по bank_client_id
```java
List<UserBankClient> clients = userBankClientRepository.findByBankClientId(bankClientId);
```

### Получить учетные данные по login
```java
Optional<UserCredentials> credentials = userCredentialsRepository.findByLogin(login);
```

### Получить все токены пользователя
```java
List<Token> tokens = tokenRepository.findByUserId(userId);
```

### Получить токен по refresh token
```java
Optional<Token> token = tokenRepository.findByRefreshToken(refreshToken);
```

### Удалить истекшие токены
```java
tokenRepository.deleteByExpiresAtBefore(LocalDateTime.now());
```

---

## ✅ Чек-лист создания Entity

Перед созданием Entity убедитесь:

- [ ] Все обязательные поля помечены `nullable = false`
- [ ] Уникальные поля помечены `unique = true`
- [ ] Для дат используется `LocalDateTime`
- [ ] Связи между сущностями настроены правильно
- [ ] Индексы созданы на часто используемых полях
- [ ] Геттеры и сеттеры добавлены для всех полей
- [ ] Методы `equals()` и `hashCode()` переопределены (опционально)
- [ ] Пароли хэшируются перед сохранением

---

## 🔐 Безопасность

### Хэширование паролей

```java
@Service
public class PasswordEncoder {
    public String encode(String rawPassword) {
        // Используйте BCrypt или SHA-256
        return BCrypt.hashpw(rawPassword, BCrypt.gensalt());
    }
    
    public boolean matches(String rawPassword, String encodedPassword) {
        return BCrypt.checkpw(rawPassword, encodedPassword);
    }
}
```

### Валидация паролей

- Минимум 8 символов
- Содержит буквы и цифры
- Можно добавить специальные символы

---

**Последнее обновление:** 2025-01-20

**Связанные документы:**
- [ARCHITECTURE.md](../ARCHITECTURE.md) - Архитектура системы
- [Java Development Guide](../guides/java-development.md) - Руководство по разработке
- [PLAN.md](../PLAN.md) - План разработки
- [Backend Module](../modules/backend.md) - Описание Backend модуля
- [Bank Module Entities](./bank-module.md) - Сущности банковского модуля

