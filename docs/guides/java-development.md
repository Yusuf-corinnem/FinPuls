# Руководство по разработке на Java для FinPuls

> **Для разработчиков, возвращающихся к Java после перерыва**

Этот документ содержит стратегию разработки, рекомендации о том, что делать самостоятельно для практики, и когда обращаться за помощью.

---

## 🎯 Общая стратегия

### Что делать самому (для практики)

#### 1. **Создание Entity классов (JPA)**
- **Начните с простых сущностей**: `User`, `UserCredentials`, `Token`
- **Практика**: аннотации `@Entity`, `@Id`, `@Column`, `@ManyToOne`
- **Время**: 1-2 часа на сущность
- **Когда обращаться**: если не помните синтаксис аннотаций или связи между сущностями

#### 2. **Создание Repository интерфейсов**
- **Простые**: `JpaRepository<User, Long>`
- **Кастомные методы**: с `@Query` (если нужны)
- **Время**: 15-30 минут на репозиторий
- **Когда обращаться**: если нужны сложные запросы или не помните синтаксис

#### 3. **Создание DTO классов**
- **Простые POJO**: с геттерами/сеттерами
- **Валидация**: через `@NotNull`, `@Size`, `@Email`
- **Время**: 20-30 минут на DTO
- **Когда обращаться**: если нужна сложная валидация или маппинг

#### 4. **Простые Service классы**
- **Бизнес-логика**: без сложных зависимостей
- **Время**: 1-2 часа на сервис
- **Когда обращаться**: если нужны транзакции, сложная логика или паттерны

---

## 🆘 Когда обращаться за помощью

### 1. **Конфигурация Spring Boot**
- Настройка DataSource, EntityManager, TransactionManager
- **Почему**: много нюансов, легко ошибиться
- **Формат**: "Помоги настроить подключение к банковской БД"

### 2. **JWT и Security**
- Генерация токенов, валидация, фильтры
- **Почему**: безопасность, много подводных камней
- **Формат**: "Помоги реализовать JwtTokenProvider"

### 3. **Middleware и фильтры**
- RequestIdFilter, TokenValidationFilter
- **Почему**: работа с HTTP, контекстом, Spring Security
- **Формат**: "Помоги создать фильтр для проверки токенов"

### 4. **Обработка ошибок**
- GlobalExceptionHandler, кастомные исключения
- **Почему**: правильная структура и обработка
- **Формат**: "Помоги настроить обработку исключений"

### 5. **Интеграция модулей**
- Инициализация Bank Module роутера
- **Почему**: специфика Spring WebFlux Router Functions
- **Формат**: "Помоги подключить Bank Module роутер"

---

## 📅 Рекомендуемый порядок работы

### Неделя 1: Доменная модель и базовая архитектура

#### День 1-2: Entity и Repository (делайте сами)
```java
// Начните с простого - User entity
@Entity
@Table(name = "users")
public class User {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @Column(nullable = false)
    private String username;
    // ... остальные поля
}
```

**Что делать:**
- Создать Entity: `User`, `UserCredentials`, `Token`
- Создать Repository: `UserRepository`, `UserCredentialsRepository`, `TokenRepository`
- Протестировать базовые CRUD операции

**Когда обращаться:**
- Если не помните синтаксис аннотаций JPA
- Если нужны сложные связи между сущностями (`@ManyToOne`, `@OneToMany`)
- Если нужны кастомные запросы в Repository

#### День 3: DTO и валидация (делайте сами, спрашивайте про сложное)
- Создайте `LoginRequest`, `LoginResponse`
- Добавьте валидацию через `@NotNull`, `@Size`, `@Email`

**Что делать:**
- Создать DTO для всех запросов/ответов
- Добавить валидацию полей
- Протестировать валидацию

**Когда обращаться:**
- Если нужна сложная валидация (кастомные валидаторы)
- Если нужен маппинг Entity ↔ DTO (MapStruct)

#### День 4-5: Service слой (делайте сами, спрашивайте про транзакции)
- `AuthService` с базовой логикой
- Обращайтесь за помощью с транзакциями и обработкой ошибок

**Что делать:**
- Создать интерфейсы сервисов (`IAuthService`)
- Реализовать базовую бизнес-логику
- Добавить обработку ошибок

**Когда обращаться:**
- Если нужны транзакции (`@Transactional`)
- Если нужна сложная обработка ошибок
- Если нужны паттерны (Factory, Strategy)

---

### Неделя 2: Security и Middleware

#### День 1-2: JWT (обращайтесь за помощью)
- "Помоги создать JwtTokenProvider"
- "Помоги настроить валидацию токенов"

**Что делать:**
- Изучить библиотеку JWT (jjwt)
- Понять структуру токенов

**Когда обращаться:**
- Создание `JwtTokenProvider`
- Генерация access/refresh токенов
- Валидация токенов
- Извлечение данных из токенов

#### День 3-4: Фильтры и Middleware (обращайтесь за помощью)
- "Помоги создать TokenValidationFilter"
- "Помоги настроить RequestIdFilter"

**Что делать:**
- Изучить Spring Security фильтры
- Понять работу с HTTP запросами

**Когда обращаться:**
- Создание `TokenValidationFilter`
- Создание `RequestIdFilter`
- Настройка Spring Security
- Работа с `SecurityContext`

#### День 5: Интеграция (обращайтесь за помощью)
- "Помоги подключить фильтры к Spring Security"

**Что делать:**
- Изучить конфигурацию Spring Security
- Понять порядок фильтров

**Когда обращаться:**
- Настройка `SecurityConfig`
- Регистрация фильтров
- Настройка публичных endpoints

---

### Неделя 3: Bank Module

#### День 1-2: Entity Bank Module (делайте сами)
- `Account`, `Card`, `Transaction`

**Что делать:**
- Создать Entity для банковского модуля
- Создать Repository
- Настроить подключение к банковской БД

**Когда обращаться:**
- Если нужна помощь с настройкой отдельной БД
- Если нужны сложные связи между сущностями

#### День 3-4: Service и Router (делайте сами, спрашивайте про Router Functions)
- "Помоги создать Router Functions для Bank Module"

**Что делать:**
- Создать сервисы для банковских операций
- Изучить Spring WebFlux Router Functions

**Когда обращаться:**
- Создание Router Functions
- Настройка обработчиков (Handlers)
- Интеграция с Backend

---

## 💬 Формат вопросов

### ✅ Хорошо:
- "Помоги создать Entity User с полями: id, username, email"
- "Помоги настроить подключение к банковской БД в PersistenceConfig"
- "Помоги реализовать метод login в AuthService"
- "Помоги создать JwtTokenProvider для генерации токенов"
- "Помоги настроить TokenValidationFilter для проверки токенов"

### ❌ Плохо:
- "Сделай все за меня"
- "Напиши весь код"
- "Как это работает?" (без контекста)

---

## 📚 Полезные ресурсы для самостоятельного изучения

### 1. **Spring Data JPA**
- Документация по аннотациям: `@Entity`, `@Repository`
- Связи между сущностями: `@ManyToOne`, `@OneToMany`, `@OneToOne`
- Кастомные запросы: `@Query`, `@NamedQuery`

### 2. **Jakarta Bean Validation**
- Базовые аннотации: `@NotNull`, `@Size`, `@Email`, `@Min`, `@Max`
- Кастомные валидаторы: `@Constraint`

### 3. **Spring Boot**
- Аннотации: `@Service`, `@Autowired`, `@Transactional`
- Конфигурация: `@Configuration`, `@Bean`
- Профили: `@Profile`

### 4. **Spring Security**
- Фильтры: `Filter`, `OncePerRequestFilter`
- Контекст: `SecurityContext`, `Authentication`
- Конфигурация: `SecurityFilterChain`

### 5. **JWT**
- Библиотека: `io.jsonwebtoken:jjwt`
- Структура токенов: Header, Payload, Signature
- Валидация и генерация

---

## ✅ Чек-лист перед обращением за помощью

Перед тем как обратиться за помощью, убедитесь:

- [ ] Прочитал документацию Spring/JPA
- [ ] Попробовал написать код сам
- [ ] Понял, в чем конкретно проблема
- [ ] Подготовил контекст (что уже сделано, что не работает)
- [ ] Сформулировал конкретный вопрос

---

## 🔄 Пример работы

### Сценарий 1: Создание Entity User

1. **Вы**: Создаете Entity `User` сами
   ```java
   @Entity
   @Table(name = "users")
   public class User {
       @Id
       @GeneratedValue(strategy = GenerationType.IDENTITY)
       private Long id;
       
       @Column(nullable = false)
       private String username;
   }
   ```

2. **Вы**: Создаете `UserRepository` сами
   ```java
   public interface UserRepository extends JpaRepository<User, Long> {
   }
   ```

3. **Вы**: Обращаетесь: "Помоги настроить `PersistenceConfigMain` для работы с User entity"

4. **Я**: Помогаю с конфигурацией

5. **Вы**: Тестируете и продолжаете работу

---

### Сценарий 2: Создание AuthService

1. **Вы**: Создаете интерфейс `IAuthService` сами
   ```java
   public interface IAuthService {
       LoginResponse login(LoginRequest request);
   }
   ```

2. **Вы**: Создаете базовую реализацию `AuthService` сами
   ```java
   @Service
   public class AuthService implements IAuthService {
       // базовая логика
   }
   ```

3. **Вы**: Обращаетесь: "Помоги с транзакциями в методе login"

4. **Я**: Помогаю с `@Transactional` и обработкой ошибок

5. **Вы**: Дорабатываете и тестируете

---

## 🎓 Принципы обучения

### 1. **Начинайте с простого**
- Сначала простые Entity без связей
- Потом добавляйте связи
- Затем сложную логику

### 2. **Пишите код сами**
- Даже если не уверены - попробуйте
- Ошибки - это нормально
- Учитесь на ошибках

### 3. **Спрашивайте конкретно**
- Не "как это работает?"
- А "как создать Entity с полем email типа String?"

### 4. **Тестируйте каждый шаг**
- После создания Entity - проверьте
- После создания Repository - проверьте
- Не накапливайте проблемы

### 5. **Изучайте документацию**
- Spring Data JPA документация
- Spring Boot документация
- Jakarta Bean Validation документация

---

## 📝 Шаблоны кода для начала

### Entity (простой пример)
```java
@Entity
@Table(name = "users")
public class User {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @Column(nullable = false, unique = true)
    private String username;
    
    @Column(nullable = false)
    private String email;
    
    @Column(name = "first_name")
    private String firstName;
    
    @Column(name = "last_name")
    private String lastName;
    
    @Column(name = "created_at")
    private LocalDateTime createdAt;
    
    @Column(name = "updated_at")
    private LocalDateTime updatedAt;
    
    // Геттеры и сеттеры
}
```

### Repository (простой пример)
```java
@Repository
public interface UserRepository extends JpaRepository<User, Long> {
    Optional<User> findByUsername(String username);
    Optional<User> findByEmail(String email);
}
```

### DTO (простой пример)
```java
public class LoginRequest {
    @NotNull(message = "Login is required")
    @Size(min = 3, max = 50, message = "Login must be between 3 and 50 characters")
    private String login;
    
    @NotNull(message = "Password is required")
    @Size(min = 6, message = "Password must be at least 6 characters")
    private String password;
    
    // Геттеры и сеттеры
}
```

### Service (простой пример)
```java
@Service
public class AuthService implements IAuthService {
    
    private final UserRepository userRepository;
    private final PasswordEncoder passwordEncoder;
    
    @Autowired
    public AuthService(UserRepository userRepository, PasswordEncoder passwordEncoder) {
        this.userRepository = userRepository;
        this.passwordEncoder = passwordEncoder;
    }
    
    @Override
    public LoginResponse login(LoginRequest request) {
        // Ваша логика здесь
        return new LoginResponse();
    }
}
```

---

## 🚀 Следующие шаги

1. **Начните с Entity User**
   - Создайте класс с базовыми полями
   - Добавьте аннотации JPA
   - Протестируйте

2. **Создайте Repository**
   - Простой интерфейс, расширяющий `JpaRepository`
   - Добавьте кастомные методы при необходимости

3. **Создайте DTO**
   - Request и Response классы
   - Добавьте валидацию

4. **Создайте Service**
   - Интерфейс и реализация
   - Базовая бизнес-логика

5. **Обращайтесь за помощью**
   - Когда нужна конфигурация
   - Когда нужна сложная логика
   - Когда не уверены в правильности решения

---

## 📌 Важные напоминания

- **Не бойтесь ошибок** - они часть обучения
- **Пишите код сами** - это лучший способ учиться
- **Спрашивайте конкретно** - так быстрее получите помощь
- **Тестируйте каждый шаг** - не накапливайте проблемы
- **Изучайте документацию** - она ваш лучший друг

---

**Последнее обновление:** 2025-01-20

**Связанные документы:**
- [ARCHITECTURE.md](../ARCHITECTURE.md) - Архитектура системы
- [Development Guide](./development.md) - Руководство по разработке
- [PLAN.md](../PLAN.md) - План разработки
- [Backend Entities](../entities/backend.md) - Сущности Backend модуля
- [Bank Module Entities](../entities/bank-module.md) - Сущности Bank Module

