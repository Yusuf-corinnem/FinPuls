# Архитектура FinPuls - Финансовый Пульсометр

## 📋 Содержание
1. [Общее описание](#общее-описание)
2. [Технологический стек](#технологический-стек)
3. [Архитектура системы](#архитектура-системы)
4. [Архитектура Backend](#архитектура-backend)
5. [Архитектура Bank Service](#архитектура-bank-service)
6. [Архитектура Frontend](#архитектура-frontend)
7. [Структура проекта](#структура-проекта)
8. [Компоненты системы](#компоненты-системы)
9. [Паттерны проектирования](#паттерны-проектирования)
10. [API Формат ответов](#api-формат-ответов)
11. [База данных](#база-данных)
12. [Docker и развертывание](#docker-и-развертывание)

---

## 🎯 Общее описание

**FinPuls** — финансовое приложение с изолированным банковским сервисом. Backend выступает в роли API Gateway между клиентом и банком, обеспечивая авторизацию, валидацию и проксирование запросов (пока что банк - это просто отдельынй модуль, который в дальнейшем можно вынести в микросервис).

### Ключевые особенности:
- **Изолированный Bank Module** — отдельный модуль внутри проекта с полной изоляцией (свои конфиги, сервисы, логирование, метрики), готовый к выносу в микросервис
- **Backend как прослойка** — API Gateway между клиентом и банком, инициализирует роутер Bank Module
- **Авторизация** — логин/пароль с JWT токенами
- **Две БД** — PostgreSQL для основных данных, H2 для токенов
- **Middleware для токенов** — автоматическая проверка авторизации

### Архитектурная схема

```
Frontend → Backend API (Gateway) → Bank Module (внутри проекта) → PostgreSQL
                ↓
            H2 (tokens)
```

**Примечание:** Bank Module находится внутри того же проекта, но полностью изолирован. В будущем его можно легко вынести в отдельный микросервис, просто указав URL банка.

---

## 🛠 Технологический стек

### Backend (API Gateway)
- **Java 21**
- **Spring Boot 3.x**
- **Spring Data JPA** — работа с БД
- **PostgreSQL** — основная БД (пользователи)
- **H2 Database** — in-memory БД для хранения токенов
- **Spring Security** — безопасность и JWT
- **Spring AOP** — аспекты для обертки ответов
- **MapStruct** — маппинг Entity ↔ DTO
- **Jakarta Bean Validation** — валидация запросов
- **Spring WebFlux / RouterFunction** — инициализация роутера Bank Module
- **SpringDoc OpenAPI** — Swagger документация
- **Micrometer + Prometheus** — метрики
- **SLF4J + Logback** — логирование
- **Java Crypto** — хэширование паролей

### Bank Module (изолированный модуль)
- **Java 21**
- **Spring Boot 3.x** (как модуль, не отдельное приложение)
- **Spring Data JPA** — работа с БД
- **PostgreSQL** — БД для банковских данных (счета, карты, транзакции)
- **REST API** — изолированный модуль с полной автономией
- **Собственные конфиги** — отдельные application.properties
- **Собственное логирование** — отдельные логи
- **Собственные метрики** — отдельные метрики Prometheus

### Frontend
- **React 18+** с TypeScript
- **Vite** — сборка
- **Axios** — HTTP клиент
- **React Query / SWR** — кэширование и синхронизация данных
- **Recharts / Chart.js** — графики и визуализация
- **Tailwind CSS** — стилизация

### Инфраструктура
- **Docker** + **Docker Compose**
- **Maven** — сборка backend
- **Prometheus** — сбор метрик (опционально)
- **Nginx** — reverse proxy (опционально)

---

## 🏗 Архитектура системы

### Общая схема

```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend                              │
│                    (React + TypeScript)                      │
└──────────────────────────┬──────────────────────────────────┘
                            │ HTTP/REST
                            │
┌──────────────────────────▼──────────────────────────────────┐
│              Backend API (FinPuls Gateway)                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Middleware Layer:                                    │  │
│  │  - TokenValidationFilter (JWT проверка)              │  │
│  │  - RequestIdFilter (трассировка)                     │  │
│  │  - LoggingInterceptor (логирование)                  │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Controller Layer:                                    │  │
│  │  - AuthController (login, token mgmt)                │  │
│  │  - BalanceController                                 │  │
│  │  - CardController                                    │  │
│  │  - AccountController                                 │  │
│  │  - TransferController                                │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Service Layer:                                       │  │
│  │  - AuthService (авторизация, токены)                 │  │
│  │  - BankService (прокси к Bank Service)              │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Database Layer:                                      │  │
│  │  - PostgreSQL (users)                                  │  │
│  │  - H2 (tokens)                                        │  │
│  └──────────────────────────────────────────────────────┘  │
└──────────────────────────┬──────────────────────────────────┘
                            │ HTTP/REST
                            │
┌──────────────────────────▼──────────────────────────────────┐
│              Bank Module (внутри проекта)                    │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Router Configuration:                                 │  │
│  │  - Инициализируется из Backend                        │  │
│  │  - GET /api/bank/balances                            │  │
│  │  - GET /api/bank/cards                               │  │
│  │  - GET /api/bank/accounts                            │  │
│  │  - POST /api/bank/transfer                           │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Service Layer (изолированный):                       │  │
│  │  - AccountService                                     │  │
│  │  - CardService                                        │  │
│  │  - TransferService                                    │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Config (изолированный):                              │  │
│  │  - Собственные application.properties                │  │
│  │  - Собственное логирование                            │  │
│  │  - Собственные метрики                                │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  PostgreSQL Database                                  │  │
│  │  (accounts, cards, transactions)                     │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🏗 Архитектура Backend

### Структура пакетов

```
com.finpuls/
├── config/                      # Конфигурация
│   ├── PersistenceConfigMain.java      # PostgreSQL конфигурация
│   ├── PersistenceConfigTokenH2.java   # H2 конфигурация для токенов
│   ├── SecurityConfig.java             # Spring Security + JWT
│   ├── SwaggerConfig.java
│   ├── WebMvcConfig.java               # Интерцепторы
│   └── BankServiceProperties.java      # @ConfigurationProperties для Bank Service
│
├── domain/                      # Доменная модель
│   ├── model/                   # JPA Entities
│   │   ├── user/
│   │   │   ├── User.java        # Пользователи (PostgreSQL)
│   │   │   └── UserCredentials.java  # Учетные данные (PostgreSQL)
│   │   └── token/
│   │       └── Token.java       # Токены (H2)
│   ├── repository/              # JPA Repositories
│   │   ├── user/
│   │   │   ├── UserRepository.java
│   │   │   └── UserCredentialsRepository.java
│   │   └── token/
│   │       └── TokenRepository.java
│   └── dto/                     # Data Transfer Objects
│       ├── request/
│       │   ├── auth/
│       │   │   ├── LoginRequest.java
│       │   │   └── RefreshTokenRequest.java
│       │   ├── balance/
│       │   │   └── GetBalanceRequest.java
│       │   ├── card/
│       │   │   └── GetCardsRequest.java
│       │   ├── account/
│       │   │   └── GetAccountsRequest.java
│       │   └── transfer/
│       │       └── TransferRequest.java
│       ├── response/
│       │   ├── auth/
│       │   │   ├── LoginResponse.java
│       │   │   └── TokenResponse.java
│       │   ├── balance/
│       │   │   └── BalanceDTO.java
│       │   ├── card/
│       │   │   └── CardDTO.java
│       │   ├── account/
│       │   │   └── AccountDTO.java
│       │   └── transfer/
│       │       └── TransferResponse.java
│       └── internal/
│           └── BankServiceResponse.java
│
├── service/                     # Бизнес-логика
│   ├── auth/
│   │   ├── IAuthService.java
│   │   ├── AuthService.java
│   │   ├── ITokenService.java
│   │   └── TokenService.java
│   ├── bank/
│   │   ├── IBankService.java
│   │   └── BankService.java     # Прокси к Bank Service
│   └── common/
│       ├── AbstractCrudService.java
│       └── CrudService.java
│
├── api/                         # REST API
│   ├── controller/
│   │   ├── AuthController.java
│   │   ├── BalanceController.java
│   │   ├── CardController.java
│   │   ├── AccountController.java
│   │   └── TransferController.java
│   └── response/
│       ├── IApiResponse.java    # Интерфейс единого формата
│       ├── ApiResponse.java     # Реализация
│       ├── ResponseStatus.java  # Enum (SUCCESS/ERROR)
│       └── ErrorDetails.java    # Детали ошибок
│
├── api/exception/               # Обработка ошибок
│   ├── FinPulsException.java    # Базовое исключение
│   ├── AuthException.java
│   │   ├── InvalidCredentialsException.java
│   │   ├── TokenExpiredException.java
│   │   └── TokenInvalidException.java
│   ├── ValidationException.java
│   ├── BankServiceException.java
│   ├── GlobalExceptionHandler.java
│   └── ValidationExceptionHandler.java
│
├── middleware/                  # Middleware
│   ├── TokenValidationFilter.java    # Проверка JWT токенов
│   ├── RequestIdFilter.java
│   ├── RequestLoggingInterceptor.java
│   └── ResponseWrapperAspect.java
│
├── security/                    # Безопасность
│   ├── jwt/
│   │   ├── JwtTokenProvider.java
│   │   ├── JwtTokenValidator.java
│   │   └── JwtTokenGenerator.java
│   └── password/
│       └── PasswordEncoder.java  # Crypto хэширование
│
├── mapper/                      # MapStruct мапперы
│   ├── UserMapper.java
│   ├── TokenMapper.java
│   └── BankMapper.java
│
├── validation/                  # Кастомные валидаторы
│   └── DateRangeValidator.java
│
├── common/                      # Утилиты
│   ├── RequestContextHolder.java
│   └── LoggingHelper.java
│
└── metrics/                     # Prometheus метрики
    └── ApiMetrics.java
```

### Слои приложения

```
┌─────────────────────────────────────────────────┐
│          REST API Controllers                   │  ← API Layer
│  (AuthController, BalanceController, etc.)      │
└──────────────────┬──────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────┐
│         Middleware Layer                        │  ← Middleware
│  (TokenValidationFilter, RequestIdFilter)      │
└──────────────────┬──────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────┐
│              Service Layer                      │  ← Business Logic
│  (AuthService, BankService)                     │
└──────────────────┬──────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────┐
│          Repository Layer (JPA)                 │  ← Data Access
│  (UserRepository, TokenRepository)              │
└──────────────────┬──────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────┐
│         Databases                               │  ← Database
│  PostgreSQL (users) + H2 (tokens)               │
└─────────────────────────────────────────────────┘
```

### Компоненты Backend

#### 1. **Авторизация и аутентификация**

**AuthService:**
- Логин по login/password
- Хэширование паролей через Java Crypto (BCrypt или SHA-256)
- Генерация JWT токенов
- Валидация токенов

**TokenService:**
- Генерация access token и refresh token
- Обновление токенов (refresh)
- Удаление токенов (logout)
- Хранение refresh tokens в H2

**JWT Token Provider:**
```java
public interface JwtTokenProvider {
    String generateAccessToken(User user);
    String generateRefreshToken(User user);
    boolean validateToken(String token);
    String getLoginFromToken(String token);
}
```

#### 2. **Bank Module Integration**

**Инициализация роутера:**
- Backend регистрирует роутер Bank Module через `BankRouterConfig`
- Роуты Bank Module доступны через тот же контекст Spring Boot
- В будущем (при выносе в микросервис) заменяется на HTTP клиент

**Текущая реализация (модуль):**
```java
@Configuration
public class BankRouterConfig {
    
    @Autowired
    private BankRouter bankRouter;
    
    @Bean
    public RouterFunction<ServerResponse> bankRoutes() {
        return bankRouter.routes();
    }
}
```

**Будущая реализация (микросервис):**
```java
@FeignClient(name = "bank-service", url = "${bank.service.url}")
public interface BankServiceClient {
    @GetMapping("/api/bank/balances")
    List<BalanceDTO> getBalances();
    
    @GetMapping("/api/bank/cards")
    List<CardDTO> getCards();
    
    @PostMapping("/api/bank/transfer")
    TransferResponse transfer(TransferRequest request);
}
```

#### 3. **Middleware для токенов**

**TokenValidationFilter:**
- Проверка JWT токена в заголовке `Authorization: Bearer <token>`
- Валидация токена через JwtTokenValidator
- Установка SecurityContext с данными пользователя
- Пропуск публичных endpoints (login, health)

---

## 🏦 Архитектура Bank Module

### Принципы изоляции

Bank Module — это полностью изолированный модуль внутри проекта с минимальной связью с Backend:

1. **Полная автономия:**
   - Собственные конфиги (`application-bank.properties`)
   - Собственные сервисы и репозитории
   - Собственное логирование (отдельный файл `logs/bank-module.log`)
   - Собственные метрики Prometheus

2. **Единственная связь с Backend:**
   - Инициализация роутера через `BankRouterConfig` в Backend
   - Backend регистрирует роуты Bank Module в своем контексте

3. **Готовность к микросервису:**
   - В будущем можно легко вынести в отдельный сервис
   - Достаточно указать URL банка в конфигурации Backend
   - Все остальное остается без изменений

### Структура пакетов

```
com.finpuls.bank/
├── config/                      # Конфигурация (изолированная)
│   ├── PersistenceConfig.java   # PostgreSQL конфигурация
│   ├── BankLoggingConfig.java   # Собственное логирование
│   ├── BankMetricsConfig.java  # Собственные метрики
│   └── SwaggerConfig.java
│
├── domain/                      # Доменная модель
│   ├── model/                   # JPA Entities
│   │   ├── account/
│   │   │   └── Account.java
│   │   ├── card/
│   │   │   └── Card.java
│   │   └── transaction/
│   │       └── Transaction.java
│   ├── repository/              # JPA Repositories
│   │   ├── AccountRepository.java
│   │   ├── CardRepository.java
│   │   └── TransactionRepository.java
│   └── dto/                     # Data Transfer Objects
│       ├── request/
│       │   ├── GetBalanceRequest.java
│       │   ├── GetCardsRequest.java
│       │   ├── GetAccountsRequest.java
│       │   └── TransferRequest.java
│       └── response/
│           ├── BalanceDTO.java
│           ├── CardDTO.java
│           ├── AccountDTO.java
│           └── TransferResponse.java
│
├── service/                     # Бизнес-логика
│   ├── account/
│   │   ├── IAccountService.java
│   │   └── AccountService.java
│   ├── card/
│   │   ├── ICardService.java
│   │   └── CardService.java
│   └── transfer/
│       ├── ITransferService.java
│       └── TransferService.java
│
├── api/                         # REST API
│   ├── router/                  # Router Functions (WebFlux)
│   │   ├── BankRouter.java      # Определение роутов
│   │   ├── BalanceRouter.java
│   │   ├── CardRouter.java
│   │   ├── AccountRouter.java
│   │   └── TransferRouter.java
│   └── handler/                 # Request Handlers
│       ├── BalanceHandler.java
│       ├── CardHandler.java
│       ├── AccountHandler.java
│       └── TransferHandler.java
│
└── exception/                   # Обработка ошибок (изолированная)
    ├── BankException.java
    └── BankExceptionHandler.java
```

### Инициализация роутера в Backend

Backend инициализирует роутер Bank Module через конфигурацию:

```java
@Configuration
public class BankRouterConfig {
    
    @Autowired
    private BankRouter bankRouter;
    
    @Bean
    public RouterFunction<ServerResponse> bankRoutes() {
        return bankRouter.routes();
    }
}
```

**Важно:** Это единственная точка связи между Backend и Bank Module. В будущем, при выносе в микросервис, эта конфигурация заменяется на HTTP клиент (OpenFeign/RestTemplate).

### API Endpoints Bank Module

```
GET  /api/bank/balances          # Получить балансы
GET  /api/bank/cards             # Получить карты
GET  /api/bank/accounts          # Получить счета
POST /api/bank/transfer         # Выполнить перевод
```

### Принципы изоляции

1. **Полная автономия** — собственные конфиги, логирование, метрики
2. **Отдельная БД** — свой PostgreSQL для банковских данных
3. **Router Functions** — использование Spring WebFlux RouterFunction для изоляции
4. **Минимальная связь** — только инициализация роутера из Backend
5. **Готовность к микросервису** — легко вынести в отдельный сервис, заменив роутер на HTTP клиент

---

## 🎨 Архитектура Frontend

### Структура папок

```
frontend/
├── public/
│   └── index.html
│
├── src/
│   ├── components/              # React компоненты
│   │   ├── common/              # Переиспользуемые
│   │   │   ├── Header.tsx
│   │   │   ├── Footer.tsx
│   │   │   └── LoadingSpinner.tsx
│   │   ├── Auth/                # Авторизация
│   │   │   ├── LoginForm.tsx
│   │   │   └── ProtectedRoute.tsx
│   │   ├── Dashboard/           # Главная страница
│   │   │   ├── Dashboard.tsx
│   │   │   └── SummaryCard.tsx
│   │   ├── Balances/            # Балансы
│   │   │   ├── BalanceList.tsx
│   │   │   └── BalanceCard.tsx
│   │   ├── Cards/               # Карты
│   │   │   ├── CardList.tsx
│   │   │   └── CardItem.tsx
│   │   ├── Accounts/            # Счета
│   │   │   ├── AccountList.tsx
│   │   │   └── AccountCard.tsx
│   │   └── Transfers/           # Переводы
│   │       ├── TransferForm.tsx
│   │       └── TransferHistory.tsx
│   │
│   ├── pages/                   # Страницы
│   │   ├── LoginPage.tsx
│   │   ├── DashboardPage.tsx
│   │   ├── BalancesPage.tsx
│   │   ├── CardsPage.tsx
│   │   ├── AccountsPage.tsx
│   │   └── TransfersPage.tsx
│   │
│   ├── services/                # API клиенты
│   │   ├── api.ts               # Axios instance
│   │   ├── authService.ts
│   │   ├── balanceService.ts
│   │   ├── cardService.ts
│   │   ├── accountService.ts
│   │   └── transferService.ts
│   │
│   ├── hooks/                   # Custom hooks
│   │   ├── useAuth.ts
│   │   ├── useBalances.ts
│   │   ├── useCards.ts
│   │   └── useAccounts.ts
│   │
│   ├── store/                   # State management
│   │   ├── authStore.ts         # Zustand / Redux
│   │   └── tokenStore.ts
│   │
│   ├── types/                   # TypeScript типы
│   │   ├── api.ts               # Типы для API ответов
│   │   ├── auth.ts
│   │   ├── balance.ts
│   │   ├── card.ts
│   │   └── account.ts
│   │
│   ├── utils/                   # Утилиты
│   │   ├── formatters.ts        # Форматирование дат, денег
│   │   ├── validators.ts        # Валидация форм
│   │   └── constants.ts         # Константы
│   │
│   ├── App.tsx                  # Главный компонент
│   └── main.tsx                 # Entry point
│
├── package.json
├── tsconfig.json
├── vite.config.ts
└── tailwind.config.js
```

---

## 📁 Структура проекта

```
FinPuls/
├── backend/                     # Backend API Gateway
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/com/finpuls/
│   │   │   │   ├── [структура пакетов Backend]
│   │   │   │   └── config/
│   │   │   │       └── BankRouterConfig.java  # Инициализация роутера Bank Module
│   │   │   └── resources/
│   │   │       ├── application.properties
│   │   │       ├── application-dev.properties
│   │   │       ├── application-prod.properties
│   │   │       └── logback-spring.xml
│   │   └── test/
│   ├── pom.xml
│   └── Dockerfile
│
├── bank-module/                # Bank Module (изолированный модуль)
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/com/finpuls/bank/
│   │   │   │   └── [структура пакетов Bank Module]
│   │   │   └── resources/
│   │   │       ├── application-bank.properties  # Собственные конфиги
│   │   │       └── logback-bank.xml              # Собственное логирование
│   │   └── test/
│   └── pom.xml                 # Зависимость от backend (только для инициализации роутера)
│
├── frontend/
│   ├── src/
│   │   └── [структура выше]
│   ├── public/
│   ├── package.json
│   ├── tsconfig.json
│   ├── vite.config.ts
│   └── Dockerfile
│
├── docker-compose.yml
├── .env.example
├── ARCHITECTURE.md
├── DEVELOPMENT.md
├── PLAN.md
└── README.md
```

---

## 🔧 Компоненты системы

### 1. **Единый формат ответов API**

Все ответы оборачиваются в `IApiResponse<T>`:

```typescript
interface IApiResponse<T> {
  status: "success" | "error";
  data: T;
  message: string;
  error?: {
    code: string;
    message: string;
    context?: Record<string, any>;
  };
  requestId: string;
}
```

### 2. **Request ID**

Каждый запрос получает уникальный `requestId`:
- Генерируется автоматически или из заголовка `X-Request-Id`
- Возвращается в ответе
- Используется в логах для трассировки

### 3. **Авторизация через JWT**

**Процесс авторизации:**
1. Клиент отправляет `POST /api/auth/login` с login и password
2. Backend проверяет credentials, хэширует пароль через Crypto
3. Генерируется JWT access token и refresh token
4. Refresh token сохраняется в H2
5. Access token возвращается клиенту
6. Клиент использует access token в заголовке `Authorization: Bearer <token>`

**Обновление токена:**
1. Клиент отправляет `POST /api/auth/refresh` с refresh token
2. Backend проверяет refresh token в H2
3. Генерируется новый access token
4. Возвращается новый access token

**Удаление токена:**
1. Клиент отправляет `POST /api/auth/logout`
2. Backend удаляет refresh token из H2
3. Access token становится невалидным

### 4. **Middleware для проверки токенов**

**TokenValidationFilter:**
- Проверяет наличие токена в заголовке `Authorization`
- Валидирует JWT токен через JwtTokenValidator
- Извлекает login из токена
- Загружает User из БД по login
- Устанавливает SecurityContext
- Пропускает публичные endpoints (login, health, swagger)

**Пример:**
```java
@Component
@Order(2)
public class TokenValidationFilter extends OncePerRequestFilter {
    @Override
    protected void doFilterInternal(HttpServletRequest request, 
                                   HttpServletResponse response, 
                                   FilterChain filterChain) {
        String token = extractToken(request);
        if (token != null && jwtTokenValidator.validateToken(token)) {
            String login = jwtTokenProvider.getLoginFromToken(token);
            User user = userService.findByLogin(login);
            SecurityContextHolder.getContext().setAuthentication(
                new UsernamePasswordAuthenticationToken(user, null, user.getAuthorities())
            );
        }
        filterChain.doFilter(request, response);
    }
}
```

### 5. **Логирование**

#### Конфигурация
- Уровень логирования настраивается через `logging.level.root` в `application.properties` (по умолчанию INFO)
- Поддерживаемые уровни: **INFO**, **DEBUG**, **WARN**
- Ротация файлов: автоматическое удаление логов старше **2 недель** (14 дней)
- Уровень логирования можно переопределить через переменную окружения `LOG_LEVEL`

#### Структура JSON лога
Все логи пишутся в JSON формате с единой структурой:

```json
{
  "level": "info|debug|warn",
  "message": "Log message text",
  "timestamp": "2025-01-20T10:30:45.123+0300",
  "data": {
    "http": {
      "ip": "192.168.1.1",
      "url": "/api/balances",
      "userAgent": "Mozilla/5.0..."
    },
    "source": {
      "file": "com.finpuls.service.BankService",
      "function": "getBalances",
      "line": 45
    },
    "data": {
      "userId": "user123"
    }
  },
  "error": {
    "code": "TOKEN_EXPIRED",
    "context": {
      "userId": "user123"
    },
    "message": "Token expired",
    "stackTrace": "..."
  }
}
```

#### Файлы логов

**Backend:**
- `logs/combined.log` — все логи приложения (INFO, DEBUG, WARN, ERROR)
- `logs/error.log` — только логи ошибок (ERROR)
- Ротация: `logs/combined.2025-01-20.log`, `logs/error.2025-01-20.log`
- Хранение: 14 дней, после чего автоматическое удаление

**Bank Module:**
- `logs/bank-combined.log` — все логи Bank Module (INFO, DEBUG, WARN, ERROR)
- `logs/bank-error.log` — только логи ошибок Bank Module (ERROR)
- Ротация: `logs/bank-combined.2025-01-20.log`, `logs/bank-error.2025-01-20.log`
- Хранение: 14 дней, после чего автоматическое удаление

### 6. **Метрики Prometheus**

- Количество запросов к API
- Время ответа API
- Количество ошибок
- Метрики авторизации (успешные/неуспешные логины)

---

## 🎯 Паттерны проектирования

### 1. **Repository Pattern**
JPA Repositories для абстракции доступа к данным

### 2. **Service Layer Pattern**
Бизнес-логика в сервисах, контроллеры тонкие

### 3. **Gateway Pattern**
Backend выступает как API Gateway между клиентом и Bank Service

### 4. **Proxy Pattern**
BankService проксирует запросы к Bank Service

### 5. **DTO Pattern**
Отделение Entity от API, использование интерфейсов для типизации

### 6. **AOP Pattern**
Автоматическая обертка ответов

### 7. **Filter Pattern**
Middleware фильтры для проверки токенов и логирования

---

## 📊 API Формат ответов

### Успешный ответ

```json
{
  "status": "success",
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refreshToken": "refresh_token_here",
    "expiresIn": 3600
  },
  "message": "Login successful",
  "error": null,
  "requestId": "a1b2c3d4e5f6g7h8"
}
```

### Ошибка

```json
{
  "status": "error",
  "data": null,
  "message": "Operation failed",
  "error": {
    "code": "INVALID_CREDENTIALS",
    "message": "Invalid login or password",
    "context": {
      "method": "POST",
      "uri": "/api/auth/login"
    }
  },
  "requestId": "a1b2c3d4e5f6g7h8"
}
```

### Ошибка валидации

```json
{
  "status": "error",
  "data": null,
  "message": "Operation failed",
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Validation failed",
    "context": {
      "validationErrors": {
        "login": "Login is required",
        "password": "Password must be at least 8 characters"
      },
      "method": "POST",
      "uri": "/api/auth/login"
    }
  },
  "requestId": "a1b2c3d4e5f6g7h8"
}
```

---

## 💾 База данных

### PostgreSQL (Основная БД)

**Назначение**: Хранит пользователей и банковские данные (через Bank Service).

**Таблица: users**
```sql
CREATE TABLE users (
    id BIGSERIAL PRIMARY KEY,
    username VARCHAR(100) NOT NULL UNIQUE,
    email VARCHAR(255),
    first_name VARCHAR(100),
    last_name VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**Таблица: user_credentials**
```sql
CREATE TABLE user_credentials (
    id BIGSERIAL PRIMARY KEY,
    login VARCHAR(100) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    user_id BIGINT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    UNIQUE KEY unique_user_credentials (user_id)
);
```

**Примечания:**
- `users` — основная информация о пользователе (без пароля)
- `user_credentials` — отдельная таблица для хранения логинов и паролей
- `password` — хэшированный пароль через Java Crypto (BCrypt или SHA-256)
- `login` — логин пользователя (может отличаться от username)
- `user_id` — связь с таблицей users
- Банковские данные (счета, карты, транзакции) хранятся в Bank Module

### H2 Database (Токены)

**Назначение**: Хранит refresh tokens для JWT авторизации.

**Таблица: tokens**
```sql
CREATE TABLE tokens (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    refresh_token VARCHAR(500) NOT NULL,
    expires_at TIMESTAMP NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_refresh_token (refresh_token)
);
```

**Примечания:**
- `refresh_token` — refresh token для обновления access token
- `expires_at` — время истечения refresh token
- `user_id` — связь с пользователем из PostgreSQL (только ID, не FK)

**Использование H2 для токенов:**

✅ **Плюсы для pet проекта:**
- Простота настройки (не требует отдельного сервера БД)
- Быстрая работа (in-memory или file-based)
- Минимальные зависимости
- Подходит для разработки и тестирования

⚠️ **Ограничения:**
- **In-memory H2** теряет данные при перезапуске приложения
- **File-based H2** сохраняет данные, но не предназначен для production нагрузок
- Ограниченная поддержка concurrent access
- Нет репликации и кластеризации

**Рекомендации:**
- Для **pet проекта** (текущий случай): H2 file-based — приемлемый вариант
- Для **production**: рекомендуется использовать PostgreSQL или Redis для токенов
- При выносе в production: легко мигрировать токены в PostgreSQL (та же структура таблицы)

**Конфигурация H2:**
```properties
# File-based H2 (сохраняет данные между перезапусками)
spring.datasource.token.url=jdbc:h2:file:./data/tokendb
# Или in-memory (данные теряются при перезапуске)
# spring.datasource.token.url=jdbc:h2:mem:tokendb
```

---

## ⚙️ Конфигурация

### application.properties (Backend)

```properties
# Server
server.port=8080

# PostgreSQL (Основная БД)
spring.datasource.url=jdbc:postgresql://localhost:5432/finpuls
spring.datasource.username=${DB_USERNAME:finpuls}
spring.datasource.password=${DB_PASSWORD:password}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# H2 (Токены)
spring.datasource.token.url=jdbc:h2:mem:tokendb
spring.datasource.token.driver-class-name=org.h2.Driver
spring.jpa.token.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.token.hibernate.ddl-auto=update

# JWT
jwt.secret=${JWT_SECRET:your-secret-key-here}
jwt.expiration=${JWT_EXPIRATION:3600}
jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION:86400}

# Bank Module (в будущем - микросервис)
# Пока что модуль внутри проекта, роутер инициализируется через BankRouterConfig
# В будущем: bank.service.url=${BANK_SERVICE_URL:http://localhost:8081}

# Logging
logging.level.root=${LOG_LEVEL:INFO}
logging.level.com.finpuls=DEBUG
logging.file.combined.name=logs/combined.log
logging.file.error.name=logs/error.log
logging.file.error.level=ERROR

# Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Actuator (Prometheus)
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=always
management.metrics.export.prometheus.enabled=true
```

### application-bank.properties (Bank Module)

```properties
# PostgreSQL (Банковские данные)
spring.datasource.bank.url=jdbc:postgresql://localhost:5432/bankdb
spring.datasource.bank.username=${DB_USERNAME:bank}
spring.datasource.bank.password=${DB_PASSWORD:password}
spring.datasource.bank.driver-class-name=org.postgresql.Driver
spring.jpa.bank.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.bank.hibernate.ddl-auto=update

# Logging (собственное логирование Bank Module)
logging.level.com.finpuls.bank=${LOG_LEVEL:INFO}
logging.file.bank.combined.name=logs/bank-combined.log
logging.file.bank.error.name=logs/bank-error.log
logging.file.bank.error.level=ERROR

# Metrics (собственные метрики Bank Module)
management.metrics.bank.enabled=true
```

### Переменные окружения

- `LOG_LEVEL` — уровень логирования (INFO, DEBUG, WARN). По умолчанию: INFO
- `SPRING_PROFILES_ACTIVE` — активный профиль (dev, prod)
- `DB_USERNAME` — имя пользователя PostgreSQL
- `DB_PASSWORD` — пароль PostgreSQL
- `JWT_SECRET` — секретный ключ для JWT
- `JWT_EXPIRATION` — время жизни access token (секунды)
- `JWT_REFRESH_EXPIRATION` — время жизни refresh token (секунды)
- `BANK_SERVICE_URL` — URL Bank Service

---

## 🐳 Docker и развертывание

### docker-compose.yml

```yaml
version: '3.8'

services:
  postgres-main:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: finpuls
      POSTGRES_USER: finpuls
      POSTGRES_PASSWORD: ${DB_PASSWORD:password}
    ports:
      - "5432:5432"
    volumes:
      - postgres-main-data:/var/lib/postgresql/data
    networks:
      - finpuls-network

  postgres-bank:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: bankdb
      POSTGRES_USER: bank
      POSTGRES_PASSWORD: ${DB_PASSWORD:password}
    ports:
      - "5433:5432"
    volumes:
      - postgres-bank-data:/var/lib/postgresql/data
    networks:
      - finpuls-network

  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - LOG_LEVEL=${LOG_LEVEL:INFO}
      - DB_USERNAME=finpuls
      - DB_PASSWORD=${DB_PASSWORD:password}
      - JWT_SECRET=${JWT_SECRET:your-secret-key}
    depends_on:
      - postgres-main
      - postgres-bank
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - finpuls-network
    # Примечание: Bank Module находится внутри backend, не отдельный контейнер

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=http://localhost:8080/api
    depends_on:
      - backend
    networks:
      - finpuls-network

  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    networks:
      - finpuls-network

volumes:
  postgres-main-data:
  postgres-bank-data:

networks:
  finpuls-network:
    driver: bridge
```

### Backend Dockerfile

```dockerfile
FROM maven:3.9-eclipse-temurin-17 AS build
WORKDIR /app
COPY pom.xml .
COPY src ./src
RUN mvn clean package -DskipTests

FROM eclipse-temurin:17-jre
WORKDIR /app
COPY --from=build /app/target/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
```

### Backend Dockerfile (включает Bank Module)

```dockerfile
FROM maven:3.9-eclipse-temurin-21 AS build
WORKDIR /app
COPY backend/pom.xml .
COPY bank-module/pom.xml ./bank-module/
COPY backend/src ./backend/src
COPY bank-module/src ./bank-module/src
RUN mvn clean package -DskipTests

FROM eclipse-temurin:21-jre
WORKDIR /app
COPY --from=build /app/backend/target/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
```

**Примечание:** Bank Module компилируется вместе с Backend, так как это модуль внутри проекта.

### Frontend Dockerfile

```dockerfile
FROM node:18-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 3000
CMD ["nginx", "-g", "daemon off;"]
```

---

## 🔐 Безопасность

### Текущая реализация
- Пароли хэшируются через Java Crypto (BCrypt или SHA-256)
- JWT токены для авторизации
- Refresh tokens хранятся в H2
- Маскировка чувствительных данных в логах
- Валидация всех входных данных
- TokenValidationFilter для проверки токенов

### Будущее
- OAuth 2.0 интеграция
- Шифрование токенов с использованием Spring Vault
- Refresh token rotation
- Rate limiting

---

## 📈 Мониторинг и метрики

### Prometheus метрики
- `api_requests_total` — количество запросов к API
- `api_errors_total` — количество ошибок
- `api_response_time_seconds` — время ответа
- `auth_login_attempts_total` — попытки входа
- `auth_login_success_total` — успешные входы
- `auth_login_failure_total` — неуспешные входы

### Swagger документация
- Доступна по адресу: `http://localhost:8080/swagger-ui.html`
- Автоматически генерируется из аннотаций

---

## 🚀 Точки расширения

1. **Вынос Bank Module в микросервис**: 
   - Модуль уже изолирован и готов к выносу
   - Заменить `BankRouterConfig` на HTTP клиент (OpenFeign)
   - Указать URL банка в конфигурации
   - Развернуть Bank Module как отдельный сервис
2. **Добавление новых операций**: Расширить Bank Module API
3. **OAuth интеграция**: Добавить OAuth 2.0 flow
4. **Кэширование**: Добавить Redis для кэширования
5. **WebSocket**: Real-time обновления балансов и транзакций
